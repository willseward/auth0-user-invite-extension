import React, { PropTypes, Component } from 'react';
import connectContainer from 'redux-static';

export default class InvitationEmail extends Component {

  render() {

    return (
      <div>
        Invitation Email Area
      </div>
    );
  }
};
